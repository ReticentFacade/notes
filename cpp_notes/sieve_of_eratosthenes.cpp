#include<bits/stdc++.h>
using namespace std;

int countPrimes(int n) {
  int count = 0; 
  if (n <= 1) return 0;

  bool *prime = (bool*) malloc((n + 1) * (sizeof(bool));
  for {
    prime[i] = true;
  }

  prime[0] = prime[1] = true;

}

int main() {}
